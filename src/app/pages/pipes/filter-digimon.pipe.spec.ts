import { FilterDigimonPipe } from './filter-digimon.pipe';

describe('FilterDigimonPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDigimonPipe();
    expect(pipe).toBeTruthy();
  });
});
